

/**
 *    域名通过dns解析获得ip地址 也就是服务器的地址 ->请求下来一个html页面，遇到link就去服务器下载css文件 遇到 script标签就去下载js文件， -
    浏览器内核解析html文件时，HTMLparse模块会解析html模块，cssParse模块解析css
    对于js的解析与执行，js引擎中的parse木块会将代码转化成AST树结构，AST又会经过Ignation模块转成bytecode字节码，最后转成了cup能识别的机器语言，通过前面的解析与执行，最终会形成一个renderTree之后通过layout根据不同的环境来进行不同的布局
    <!-- 浏览器引擎指的就是浏览器的内核 -->
    浏览器内核由两部分组成
    一个是负责解析、布局、渲染html等等相关的工作
    一个是解析并执行js代码


    js引擎的原理
    js引擎中的parse会将代码转化成AST树结构，AST又会经过Ignation转成bytecode字节码，然后最终转成了cup能识别的机器语言

 * 
 * 
 */
 